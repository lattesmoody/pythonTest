# 3-1. if __name__ == "__main__"

# 객체 참조 시 "지역" -> "글로벌" -> "빌트인"

# if __name__ == "__main__" 요거 명시되어 있는 파일을 직접실행하면 해당 라인의 아래에 존재하는 함수가 실행된다.
# 그러나 다른 py파일에서 해당 글이 명시된 py를 불러오면? 아까 아래에 존재하는 함수는 실행되지 않는다.

